#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
/*  int topla(int sayi){
    /* while toplama
    int top = 0;
    int  i = 1;
    int n;

    while (i<=sayi)
    {
        top += i;
        i++; 
    }
    
    printf("%d",top); */

/* for ile toplama
    int top = 0;
    int i;
    for (i=1;i<=sayi;i++){

        top += i;
    }
    printf("%d",top);

 };*/

/*karakter replace etme  
int replace(char dizi[100]){
    int i,a;
    a = strlen(dizi);
    puts(dizi);
    for(i=0;i<a;i++){
        if (dizi[i] == 'a')
        {
            dizi[i] = '#';
        }
    }
    puts(dizi);
 }

int main(){
    char dizi[100];
    puts("bir yazi girin:");
    gets(dizi);
    replace(dizi);

    getch();
    return 0;
}*/

/* for ile rakam bölme
    for (i = 3; i >= 0; i--)
    {
       rakam = N%10;
       printf("%d",rakam);
       dizi[i] = rakam;
       N = N/10;
    }
    printf("\n dizilerdeki rakamlar\n");
    for (i = 0; i < 4; i++)
    {
        printf("%d",dizi[i]);
    } */
    
/*While rakam bölme
     while (N!= 0)
    {   
       rakam = N%10;
       if (rakam == 5)
       {
           
       }
       
       N =  N/10;
       
    } */